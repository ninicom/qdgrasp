# QDGrasp-Flow scale s. Widths and depths come from FLOW_SCALES in
# qdgrasp/models/config.py; a preset names a scale, it does not restate one.
schema: qdgrasp/model/v1
name: qdgrasp-flow-s
type: qdgrasp_flow
params:
  scale: s
  voxel_size: 0.005
  extent: 0.5
  flow_steps: 5
  max_joints: 32
  grasps: 8
